name: Legacy Frontend Deploy (AWS disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Print notice
        run: |
          echo "AWS frontend deploy is disabled."
          echo "Use render.yaml to deploy milkybloom-frontend on Render."
