name: Legacy Backend Deploy (AWS disabled)

on:
  workflow_dispatch:

jobs:
  disabled:
    runs-on: ubuntu-latest

    steps:
      - name: Print notice
        run: |
          echo "AWS backend deploy is disabled."
          echo "Use render.yaml to deploy milkybloom-backend on Render."
